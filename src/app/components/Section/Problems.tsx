/**
 * Canonical Component: Section/Problems
 * Export: ProblemsSection
 * 
 * Wrapper for ProblemCards component following Figma-style nomenclature.
 * Zero Deletion Policy: Original component preserved at ../ProblemCards.tsx
 */

import { ProblemCards } from '../ProblemCards';

export const ProblemsSection = ProblemCards;

// Re-export for convenience
export { ProblemCards };
